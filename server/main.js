import { Meteor } from 'meteor/meteor';
import { Calificaciones } from '../imports/api/calificaciones.js';
import { Vfpreguntas } from '../imports/api/VFpreguntas.js';
import { Spreguntas } from '../imports/api/spreguntas.js';

Meteor.startup(() => {
  // code to run on server at startup
});
